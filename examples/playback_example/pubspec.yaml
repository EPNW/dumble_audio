name: playback_example
description: "An example for dumble_audio to demonstrate, how to play raw audio streams"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  dumble_audio:
    path: ../..
  mumble_sounds: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true